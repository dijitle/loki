---
kind: pipeline
type: kubernetes
name: default

steps:
  - name: build
    image: docker:dind
    privileged: true
    volumes:
      - name: dockersock
        path: /var/run/
    commands:
      - docker build -f cmd/fluent-bit/Dockerfile --tag dijitle/fluent-bit-plugin-loki:$DRONE_COMMIT .
  - name: push
    image: docker:dind
    privileged: true
    environment:
      USERNAME:
        from_secret: docker_username
      PASSWORD:
        from_secret: docker_password
    volumes:
      - name: dockersock
        path: /var/run/
    commands:
      - docker tag dijitle/fluent-bit-plugin-loki:$DRONE_COMMIT dijitle/fluent-bit-plugin-loki:1.3
      - docker login -u $USERNAME -p $PASSWORD
      - docker push dijitle/fluent-bit-plugin-loki:1.3

volumes:
  - name: dockersock
    host:
      path: /var/run/
---
kind: signature
hmac: 7b8b9d44d423e83a033e62f1c29212b9832d951fe2a9c8ba0a84e786e449c739

...
