---
kind: pipeline
type: kubernetes
name: default

steps:
  - name: build
    image: docker:dind
    privileged: true
    volumes:
      - name: dockersock
        path: /var/run/
    commands:
      - docker build -f cmd/fluent-bit/Dockerfile --tag dijitle/fluent-bit-plugin-loki:$DRONE_COMMIT .
  - name: push
    image: docker:dind
    privileged: true
    environment:
      USERNAME:
        from_secret: docker_username
      PASSWORD:
        from_secret: docker_password
    volumes:
      - name: dockersock
        path: /var/run/
    commands:
      - docker tag dijitle/fluent-bit-plugin-loki:$DRONE_COMMIT dijitle/fluent-bit-plugin-loki:0.1
      - docker login -u $USERNAME -p $PASSWORD
      - docker push dijitle/fluent-bit-plugin-loki:0.1
    when:
      event:
        - tag
volumes:
  - name: dockersock
    host:
      path: /var/run/
---
kind: signature
hmac: de0f0b46d23c26baf882ea0ecf8c1b4117a2e3027a06df7896b2afd289758f82

...
